Browser-backed Sounding tests live here.

This lane is intentionally reserved for `{ browser: true }` flows only.
If a behavior can be proven with `get()`, `post()`, `visit()`, or `auth.request.*`,
it belongs in `tests/functional/pages` instead.
